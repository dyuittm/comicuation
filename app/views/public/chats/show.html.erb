<div class="container">
  <div class="row">
    <div class="col-xs-6">
      <h2>CHAT WITH <%= @group.name %></h2>

      <table class="message table">
        <thead>
          <tr>
            <th style="text-align: left; font-size: 20px;"><%= current_user.user_name %></th>
            <th style="text-align: right; font-size: 20px;"><%= @group.user.user_name %></th>
          </tr>
        </thead>
        <% @chats.each do |chat| %>
          <% if chat.group.user_id == group.user.id %>
          <tbody>
            <tr>
              <th>
                <p style="text-align: left;"><%= chat.message %></p>
              </th>
              <th></th>
            </tr>
          <% else %>
            <tr>
              <th></th>
              <th>
                <p style="text-align: right;"><%= chat.message %></p>
              </th>
            </tr>
          </tbody>
          <% end %>
        <% end %>
      </table>

      <%= form_with model: @chat, remote: true do |f| %>
        <%= f.text_field :message %>
        <%= f.hidden_field :group_id %>
        <%= f.submit "SEND", class:"btn btn-sm btn-success chat-btn" %>
      <% end %>
    </div>
  </div>
</div>